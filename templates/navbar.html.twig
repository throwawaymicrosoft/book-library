<header>
    <nav class="navbar navbar-expand-md navbar-dark bg-light">
        <div class="collapse navbar-collapse justify-content-end" id="navbarCollapse">
            <ul class="navbar-nav">
                <li class="nav-item">
                    {% if app.user == null %}
                        <a class="btn btn-outline-success" href="{{ path('fos_user_security_login') }}">
                            Войти
                        </a>
                    {% else %}
                        <a class="btn btn-outline-danger" href="{{ path('fos_user_security_logout') }}">
                            Выйти [{{ app.user.username }}]
                        </a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </nav>
</header>